# VYBZ // ARCADE SYSTEM 01.04 PRODUCTION BACKEND CONFIGURATION

# Database (PostgreSQL via Prisma)
DATABASE_URL=

# OpenAI API (Server-Only — NEVER expose to browser via NEXT_PUBLIC_)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000
